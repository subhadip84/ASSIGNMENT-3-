# Using the Math Module for Calculations

from math import *

# take the user value from input
n=int(input("Enter a number: "))
print("Square root:",sqrt(n))
print("Logarithm:",log(n))
print("Sine:",sin(n))